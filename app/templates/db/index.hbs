{{model.database.statementCount}} statements.  {{page}}/{{pageCount}}
<ul>
  {{#each pageContent as |a|}}
    <li>({{a.pinkNumber}}) {{a.label}}: {{a.mathText}}</li>
  {{/each}}
</ul>
{{pagination-pager current=page count=pageCount}}
