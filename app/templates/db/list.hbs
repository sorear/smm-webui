{{pagination-pager current=effectivePage count=pageCount}}
{{#each pageContent as |run|}}
  {{#if run.statements}}
    {{statement-run statements=run.statements highlight=activeStatement}}
  {{else}}
    {{statement-list-subheading node=run.outlineNode}}
  {{/if}}
{{/each}}
